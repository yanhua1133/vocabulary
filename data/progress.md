# GRE3000 进度

更新：2026-08-03（已完成全量校验）

## 结果在哪看
- **`pdf/GRE3000-合订本.pdf`** ← 全书一册，1694 页 / 25 MB，带 List+Unit 两级书签（336 条）
- `pdf/List1.pdf` ～ `pdf/List31.pdf` ← 分册版，方便单独打印
- 开本：16 开 185×260mm 竖版
- **`out/List1.md` ～ `out/List31.md`** ← Markdown 源，可在编辑器里搜索
- 校验报告：`.venv/bin/python scripts/15_audit.py`（当前 0 问题）
- 数据源：`data/book.json`（原书结构）、`data/words.json`（每词字段）

## 状态：全部完成并通过校验

| 项目 | 数字 |
|---|---|
| List 文件 | 31 个 |
| 词条行 / 例句行 | 34658 / 34658 |
| 词头 | 3032 |
| 不同单词（含近/反义词） | 11523 |
| 空单元格 | **0** |
| 缺音标 | **0** |
| 机器校验问题 | **0** |
| 极其罕见（单词列删除线） | 1291 |

## 这轮校验做了什么
1. **机器校验**（`scripts/15_audit.py`，全量 11523 词）：拼写、音标字符集与 cmudict 一致性、
   例句是否完整句/含该词/有中译、短语是否含该词、词性缺失、释义缺失或混入英文、罕见标记一致性。
2. **修了 2196 个硬错误**：原书例句解码后普遍丢空格、被截断、以小写开头，一律弃用改自写例句。
3. **全量语义复核**（144 批 × 80 词，24 个 agent 两波）：逐条核对释义准确性、
   `related_to` 语境是否对、翻译是否准确、词性、口语度合理性，共修正约 2350 词。
4. **修了三个管线级 bug**（比逐词改更彻底）：
   - 词头字号阈值：长词头被压缩到 8.4-9.7pt，原来只认 10pt，漏掉约 110 个词头，
     它们的释义和近反义词被并进了上一个词条（如 `disinclination` 并进 `dignified`）。已修，词头 2967→3032。
   - cmudict→IPA 转换：重音符号跨辅音错位（`kəˈnsuːm`）、非重读 ER 写成 `ɜːr`、重读 AH 写成 `ə`。
     已按重音区分元音写法并用最长合法起首簇定位重音。
   - 原书粘连整句被当成词条（`ofothermen`、`Ibarelyremember`）：用「切分含多个虚词」判据 + 黑名单剔除。

## 格式
7 列：单词 / 音标 / 常用 / 口语 / 词性 / 中文释义 / 常用短语。
例句不占列，紧跟在该词下面、跨整行显示（内嵌 HTML 表格 + `colspan`）。
- 词头加粗；近义词 `↳近`、反义词 `↳反`
- 星级：`★`=一颗，`☆`=半颗；不补尾部空星；最低分为半颗星 `☆`
- 单词列被 `~~~~` 包住 = 极其罕见（wordfreq Zipf < 1.5）
- 词性用中文全称（形容词、及物动词…）
- 常用短语格内两行：英文搭配 + 中文
- 例句 = 英文完整句 + 中文翻译，一律自写，不用原书例句
- 每个词的「词条行 + 例句行」装在同一个 `<tbody>`，PDF 打印时整组不跨页
- **PDF 版**额外略去常用度只有半颗星（`☆`）的近义词/反义词行（词头一律保留）；
  Markdown 版保留全部，两者内容差异仅此一项

## 后续如需改动
改 `scripts/12_render.py` 后跑 `12_render.py` 重渲染 Markdown，再跑 `18_pdf.py` 重出 PDF，
不用重跑模型
（模型字段缓存在 `data/enrich_cache.json`，11641 条）。
