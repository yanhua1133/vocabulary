# 牛津搭配词典 进度

更新：2026-08-05

## 结果在哪看
- `out/牛津搭配词典.md` ← 四列：**词头 / 搭配 / 中文 / 例句**，76752 行。
  84037 行。搭配写**完整形式**（`hastily abandon`、`abandon sb to their fate`、
  `be found abandoned`），不留原书的省略写法和 `~`；一格多条就格内换行。
  例句里把当前这条搭配加粗。「类型」「义项」两列去掉了——查搭配时用不上。
- `_sample.pdf` ← 样张（前 200 行，8 页）
- `data/book.json` ← 结构化骨架：词头 → 义项 → 搭配组
- `data/groups.json` ← 在骨架上又把每组拆成小类 `{words, cn}` 和例句
- `data/ocr/pNNN.json` ← 2024 页整页 OCR，带每行 bbox

当前规模：**词头 7948，搭配组 26292，小类 98604，展开出 164836 条完整搭配**。
其中高频 150 个词头（4081 条）已经模型校对完毕。

## 原书情况
- Pdg2Pic 生成的**纯扫描件**，2058 页、499 MB，零文字层，只能 OCR（原书不入库）。
- 正文 PDF 第 27 页（书内 1）到第 2050 页（书内 2024）。
- 双栏。每栏四类行，靠「顶格 + 行首长相」就能分开，比习语词典好认得多：
  - 词头：`ability noun`（小写词 + 词性）
  - 义项：`1 skill/power to do sth 能力`（数字打头）
  - 搭配组：`ADJ.` / `ADV.` / `VERB + NOUN` / `PREP.` / `PHRASES` / `QUANT.`（全大写标签打头）
  - 续行一律缩进

## 已完成
```
cd oxford-collocations
../.venv/bin/python scripts/01_ocr.py 27 2050    # 整页 OCR（2024 页，约 15 分钟）
../.venv/bin/python scripts/03_gapfill.py 27 2050 # 补回 Vision 漏掉的整行（补了 3367 行）
../.venv/bin/python scripts/02_entries.py         # 抽骨架 → data/book.json
../.venv/bin/python scripts/03_split.py           # 拆小类和例句 → data/groups.json
../.venv/bin/python scripts/04_expand.py          # 补成完整搭配 → data/expanded.json
../.venv/bin/python scripts/07_render.py          # 渲染 → out/牛津搭配词典.md
```

## 抽取判据踩的坑
1. **`\b` 把带点词性全废了**：`HEAD_RE` 末尾写 `\b`，而 POS 里的 `\.?` 会吃掉那个点，
   `adj.` 之后是句点+行尾构不成单词边界 —— 结果 adj./adv./prep. 的词头全军覆没
   （词头 5596 → 去掉 `\b` 后 6501）。
2. **正则交替要长的在前**：`ad` 排在 `adv`/`adj` 前面会先吃掉前缀再卡住。
3. **标签和搭配词之间不一定有空格**：OCR 出来常是 `ADJ.draft`，
   点后面的空格必须可选，否则整组被并进义项文本（搭配组 17790 → 20711）。
4. **别把 NUMBER 当组标签**：例句里换行开头的 number 会被误判，还会把真组腰斩（12 处）。
5. **跨页续尾要接上**：每页各扫各的，页首那些续行没有归属就被丢了。
   现在标成 `cont` 交给主循环拼回上一页最后一块。

## 抽样复核（独立 agent 看扫描图逐条对，书内 p33 / p700）
- **词头：召回 100%、精确 100%**（5/5）
- 搭配组：召回 87.5%，串位/伪造 5 处 —— 上面第 3、4、5 条就是据此修的，修完重跑未再复核
- 注意：那轮报的「组正文 200 字符截断」是**我导出样本时截的**，数据本身完整（中位 165 字、最长 4281 字）

## 拆分踩的坑
- 例句引导符 `◇` **最常被 OCR 并成中文句号**（`协议 。 They are working…`），
  只认 `•◎*` 的话例句只能抽到 11%；补上「中文 + 句号 + 大写英文」这条后到 60%。
  句号前还可能有空格，后行断言要写成 `(?<=[\u4e00-\u9fff])\s*[。.;:]\s*`。
- **得先摘例句再切小类**。反过来的话，例句里的标点会把小类切得稀碎（90818 → 76196）。
- 代替词头的 `~` 常被认成汉字「一」，夹在英文中间时要还原——不还原的话
  「例句里不含汉字」这条边界判据就断了。

## 词头漏识别：一个基线取错，1673 个词头集体消失
子 agent 报告 `pure please`、`lucrative Care`、`solo CareS` 这些病句，查下来是
`pleasure`、`career` 整个词条的搭配都挂到了前一个词头上。根子在**顶格基线取了最小值**：
`pleasure noun` 的 x=0.063，而那一栏有个异常行 x=0.05，差 0.013 超过阈值 0.012，
词头就被当成缩进续行丢了。改用 15% 分位当基线后词头 6092 → 7765。

连带修的两个：
- **页眉过滤**：页眉是 `pleased 1282`（书眉词 + 页码）在 y≈0.06，阈值卡 0.045 全漏。
  但也不能单纯按 y 切——切到 0.08 会把栏顶的词头（`can noun`）一起切掉，
  只能认「页顶 + 带页码数字」的那种。
- **假词头**：`more`、`S`、`got a`、`has helped reduce the`、`AVOCADD t` 这些是从正文里
  捞出来的。规则：最多两个词、每个词至少两个字母、都不能是虚词
  （`baby carriage`、`black market` 要留住）。最终词头 7948。

## 词头 OCR 纠错
子 agent 报 `ow point`、`ife point`、`biock`、`coai` 这些——词头一错整个词条就查不到。
两类毛病：首字母被吃掉（`djective`→`adjective`、`elegate`→`delegate`）、
形近字母认错（`biock`→`block`、`assauit`→`assault`、`cuiture`→`culture`）。
用 wordfreq 兜底：原词本身不是常见词、且补首字母或换一个形近字母后候选唯一又够常见，
才改。坏词头 120 → 48（剩下的是多字母同时认错，如 `lecorative`、`commierce`）。

## 词头串位：三个连环坑
`ablaze` 名下曾经挂着 `have ablaze`、`retain ablaze`——那些其实是 `ability` 的搭配。
根子是合并碎行时没保护词头，连修三次才干净：
1. 词头片段**不能并进上一行**（`ablaze ad.` 被并进了 abhorrent 的 VERBS 行）。
2. 词头行**也不能把后面的碎片吸进来**——`ablaze adj.` 吞掉 ability 的 ADJ 续行后，
   它在栏内的位置就跑到 ability 前面去了，把人家十几条搭配全领走。
3. **补回来的行只能当续行，除非整行干干净净只有「词 + 词性」**。
   `ablaze adj. * đ „*x c` 就是 gapfill 补出来的乱码，还落在右栏中间。
   一刀切地不让补行开词条会丢 738 个真词头，所以加了「整行无尾巴」这个条件。

## 眼下的质量水位
- **词头层面很干净**：独立 agent 核了两页，召回和精确都 100%。
- **短组拆得很好**（`ADV hastily | 仓促离开 + 例句`）。
- **长组还乱**，样张上看得很清楚，三种表现：
  ① ~~例句重复贴到组内每一行~~ **已修**：`take_examples` 留下的 `｜EX｜` 占位符
     标着例句在原文里的位置，切完小类按占位符个数顺序分配，谁的例句归谁。
  ② ~~例句中译一路吃到下一个小类~~ **已修**：中译必须以句末标点收尾、中间不许有
     小类分隔符。原来写成 `[^A-Z]{0,80}`，例句后面拖着
     `／ effectively, largely virtually 事实上终止…` 这种尾巴。
  ③ 中文列串进例句：`◇` 还常被认成一个孤零零的 `o`（`几乎停止o This principle…`），
     补上这个变体后残留从 6665 降到 4655，**还没清完**。
  ④ ~~断词连字符~~ **已修**：`Trad- itional` → `Traditional`、`punish– ment` →
     `punishment`。要在**跨行拼接之后**抹——断词的两半本来就在两行上，
     只看单行的 normalize 够不着。
  ⑤ 少量行带 OCR 垃圾（`computer: * PC abbreviation`）。
  根子在 OCR——原书的排版分隔信息在扫描件里本来就丢了一部分，纯规则到这儿差不多到顶。

## 版面：rowspan 会让 td.cellIndex 左移
排版脚本原来按 `td.cellIndex` 取每列的行数上限和字号下限。有 rowspan 时，
**没有词头格的那些行 cellIndex 会整体左移一位**——搭配列被当成词头列，
按「最多两行、可以缩到 2.9pt」处理，于是多条搭配的格子被压成一团蚂蚁。
改成渲染时给每个 td 打 `c1`~`c4` 的 class，脚本按 class 认列。
修完样张里小于 5.5pt 的字符占比 0.0%。

另外两条：
- **每列的行数上限按内容定**：搭配列 14 行（一格能有八九条搭配，竖排本来就该占那么多），
  解释和例句 3 行，词头 2 行。宁可行高参差，也不要小到没法读。
- **词头列不能用 rowspan**：`system` 这种词条几百行，rowspan 一跨页，Chrome 会把
  那一格的边框重画一遍，第一、二列之间冒出两条竖线（x=81.0 和 81.8 并排），
  格子高度还算错、竖线直接戳出表格底边。改成每行都出一个 td，
  空的那些加 `.cont` 去掉上边框——看着照样是合并的，没有跨页毛病。
- **解释和例句一律压两行**：给三行的余地，脚本一看放得下就不缩字号了，
  结果 `An ancient irrigation system still waters these terraced / fields. /
  一套古老的灌溉系统仍在浇灌这些梯田。` 排成三行，`fields.` 半截单独占一行，
  而旁边几列都只有两行。宁可字号缩一点也要压平。搭配列例外（一格能有八九条）。

## 版面
- **词头列合并单元格**（rowspan），一个词头底下多少行就跨多少行。
- **搭配列允许四行**：一格常有三四条搭配（7.4% 的格子 ≥4 条），竖排本来就该占几行，
  硬压两行只会把字缩到看不清。其余列两行。
- 字号下限按列分开定：搭配列 4.8pt、中文列 4.6pt、例句列 4.4pt。

## 例句覆盖：原书就只有三成
82575 个小类里带例句的约 30%——**这是原书的样子**，牛津搭配词典本来就只给部分
搭配配例句，不是抽取漏了。要让每条搭配都有例句只能靠模型生成 14 万条，
成本是习语词典那轮的 20 倍，得先想清楚值不值。

## 模型校对（已验证）
规则清洗到 `04_expand` 就到顶了，剩下三类毛病只能靠模型：
OCR 错字（`放 斧⋯而转向⋯` 其实是「放弃…而转向…」）、小类切分错位
（`be left ~ed` 被切成 `eft abandoned`）、例句缺七成。

已跑通：`05_batches.py`（按词频挑词头切批次）→ 子 agent 按
`prompts/INSTRUCTIONS.md` 校对 → `06_merge.py` 并入 `data/cache.json`
→ `07_render.py` 优先用校对过的。

**头 150 个高频词头（4081 条）已校完，103 个批次 0 条丢弃**，
抽取结构后来又改过一轮，按新 key 重算命中 3885 条（95%）。

**key 用「词头 + 搭配文本」，不要用位置**：位置型 id（`词头|义项|组|小类`）
一是 `name` 名词和 `name` 动词会撞车，二是抽取结构一改就全错位——
这轮改了三次抽取，要不是换成文本 key，4081 条得重做三遍。

**全量成本**：93948 条是这次的 33 倍，按每 4 个 agent 半小时跑 2800 条算，
要 130 多个 agent-批次、七八轮。建议先做高频 1000-1500 个词头（约 2.5 万条），
覆盖日常绝大多数查询。

## 下一步（两条路，成本差很多）
1. **先排 PDF 交付当前版本**：拷 `oxford-idioms/scripts/13_pdf.py`，那本的版式坑
   （量行数要 print 媒体、边框粗细 Chrome 改不动、缩字号要回弹）都记在它的 progress 里。
2. **派子 agent 逐组校对**：7.6 万个小类，是习语词典（7000 条）的 10 倍量，
   按那本的批次规模要 1000+ 个批次。真要做得先想清楚值不值，
   或者只挑长组（小类 ≥ 4 的那些）校对，量能压到十分之一。

## NOTE 用法说明框还没处理
原书有整块的用法说明框（如 flower 词条下的 `Flowers` 框），目前整块跳过，后面要补。
