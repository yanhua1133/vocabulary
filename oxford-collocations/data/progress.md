# 牛津搭配词典 进度

更新：2026-08-05

## 状态：还没开工

目录下只有原书 `牛津搭配词典.pdf`（2058 页、499 MB，超 GitHub 单文件上限，不入库）。

## 原书情况
- Pdg2Pic 生成的**纯扫描件**：每页一张图，零文字层，只能 OCR。
- **2058 页**，是牛津习语词典（630 页）的 3.3 倍。OCR 时长和子 agent 批次数
  都要按这个量级重新估，别照搬。
- 结构跟习语词典不同：一个词头下面按词性/搭配类型分组（ADJ. / VERB + NOUN / PREP. …），
  是词块列表而不是一条条独立条目，抽取判据得重新设计。

## 下一步
1. 抽几页渲染成图，看清版式（单栏还是双栏、词头怎么标、搭配怎么分组）。
2. 拷 `oxford-idioms/scripts/01_ocr.py`、`03_gapfill.py` 跑通 OCR。
3. 照着版式重写抽取判据，拿扫描图派独立 agent 核对召回率和精度。

**动手前先读 `oxford-idioms/data/progress.md`**，那套管线的坑都在那儿记着。
