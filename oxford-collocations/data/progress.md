# 牛津搭配词典 进度

更新：2026-08-05

## 结果在哪看
- `out/牛津搭配词典.md` ← 四列：**词头 / 搭配 / 中文 / 例句**，76752 行。
  84037 行。搭配写**完整形式**（`hastily abandon`、`abandon sb to their fate`、
  `be found abandoned`），不留原书的省略写法和 `~`；一格多条就格内换行。
  例句里把当前这条搭配加粗。「类型」「义项」两列去掉了——查搭配时用不上。
- `_sample.pdf` ← 样张（前 200 行，8 页）
- `data/book.json` ← 结构化骨架：词头 → 义项 → 搭配组
- `data/groups.json` ← 在骨架上又把每组拆成小类 `{words, cn}` 和例句
- `data/ocr/pNNN.json` ← 2024 页整页 OCR，带每行 bbox

当前规模：**词头 7948，搭配组 26292，小类 98604，展开出 164836 条完整搭配**。
其中高频 150 个词头（4081 条）已经模型校对完毕。

## 原书情况
- Pdg2Pic 生成的**纯扫描件**，2058 页、499 MB，零文字层，只能 OCR（原书不入库）。
- 正文 PDF 第 27 页（书内 1）到第 2050 页（书内 2024）。
- 双栏。每栏四类行，靠「顶格 + 行首长相」就能分开，比习语词典好认得多：
  - 词头：`ability noun`（小写词 + 词性）
  - 义项：`1 skill/power to do sth 能力`（数字打头）
  - 搭配组：`ADJ.` / `ADV.` / `VERB + NOUN` / `PREP.` / `PHRASES` / `QUANT.`（全大写标签打头）
  - 续行一律缩进

## 已完成
```
cd oxford-collocations
../.venv/bin/python scripts/01_ocr.py 27 2050    # 整页 OCR（2024 页，约 15 分钟）
../.venv/bin/python scripts/03_gapfill.py 27 2050 # 补回 Vision 漏掉的整行（补了 3367 行）
../.venv/bin/python scripts/02_entries.py         # 抽骨架 → data/book.json
../.venv/bin/python scripts/03_split.py           # 拆小类和例句 → data/groups.json
../.venv/bin/python scripts/04_expand.py          # 补成完整搭配 → data/expanded.json
../.venv/bin/python scripts/07_render.py          # 渲染 → out/牛津搭配词典.md
```

## 抽取判据踩的坑
1. **`\b` 把带点词性全废了**：`HEAD_RE` 末尾写 `\b`，而 POS 里的 `\.?` 会吃掉那个点，
   `adj.` 之后是句点+行尾构不成单词边界 —— 结果 adj./adv./prep. 的词头全军覆没
   （词头 5596 → 去掉 `\b` 后 6501）。
2. **正则交替要长的在前**：`ad` 排在 `adv`/`adj` 前面会先吃掉前缀再卡住。
3. **标签和搭配词之间不一定有空格**：OCR 出来常是 `ADJ.draft`，
   点后面的空格必须可选，否则整组被并进义项文本（搭配组 17790 → 20711）。
4. **别把 NUMBER 当组标签**：例句里换行开头的 number 会被误判，还会把真组腰斩（12 处）。
5. **跨页续尾要接上**：每页各扫各的，页首那些续行没有归属就被丢了。
   现在标成 `cont` 交给主循环拼回上一页最后一块。

## 抽样复核（独立 agent 看扫描图逐条对，书内 p33 / p700）
- **词头：召回 100%、精确 100%**（5/5）
- 搭配组：召回 87.5%，串位/伪造 5 处 —— 上面第 3、4、5 条就是据此修的，修完重跑未再复核
- 注意：那轮报的「组正文 200 字符截断」是**我导出样本时截的**，数据本身完整（中位 165 字、最长 4281 字）

## 拆分踩的坑
- 例句引导符 `◇` **最常被 OCR 并成中文句号**（`协议 。 They are working…`），
  只认 `•◎*` 的话例句只能抽到 11%；补上「中文 + 句号 + 大写英文」这条后到 60%。
  句号前还可能有空格，后行断言要写成 `(?<=[\u4e00-\u9fff])\s*[。.;:]\s*`。
- **得先摘例句再切小类**。反过来的话，例句里的标点会把小类切得稀碎（90818 → 76196）。
- 代替词头的 `~` 常被认成汉字「一」，夹在英文中间时要还原——不还原的话
  「例句里不含汉字」这条边界判据就断了。

## 词头漏识别：一个基线取错，1673 个词头集体消失
子 agent 报告 `pure please`、`lucrative Care`、`solo CareS` 这些病句，查下来是
`pleasure`、`career` 整个词条的搭配都挂到了前一个词头上。根子在**顶格基线取了最小值**：
`pleasure noun` 的 x=0.063，而那一栏有个异常行 x=0.05，差 0.013 超过阈值 0.012，
词头就被当成缩进续行丢了。改用 15% 分位当基线后词头 6092 → 7765。

连带修的两个：
- **页眉过滤**：页眉是 `pleased 1282`（书眉词 + 页码）在 y≈0.06，阈值卡 0.045 全漏。
  但也不能单纯按 y 切——切到 0.08 会把栏顶的词头（`can noun`）一起切掉，
  只能认「页顶 + 带页码数字」的那种。
- **假词头**：`more`、`S`、`got a`、`has helped reduce the`、`AVOCADD t` 这些是从正文里
  捞出来的。规则：最多两个词、每个词至少两个字母、都不能是虚词
  （`baby carriage`、`black market` 要留住）。最终词头 7948。

## 词头 OCR 纠错
子 agent 报 `ow point`、`ife point`、`biock`、`coai` 这些——词头一错整个词条就查不到。
两类毛病：首字母被吃掉（`djective`→`adjective`、`elegate`→`delegate`）、
形近字母认错（`biock`→`block`、`assauit`→`assault`、`cuiture`→`culture`）。
用 wordfreq 兜底：原词本身不是常见词、且补首字母或换一个形近字母后候选唯一又够常见，
才改。坏词头 120 → 48（剩下的是多字母同时认错，如 `lecorative`、`commierce`）。

## 词头串位：三个连环坑
`ablaze` 名下曾经挂着 `have ablaze`、`retain ablaze`——那些其实是 `ability` 的搭配。
根子是合并碎行时没保护词头，连修三次才干净：
1. 词头片段**不能并进上一行**（`ablaze ad.` 被并进了 abhorrent 的 VERBS 行）。
2. 词头行**也不能把后面的碎片吸进来**——`ablaze adj.` 吞掉 ability 的 ADJ 续行后，
   它在栏内的位置就跑到 ability 前面去了，把人家十几条搭配全领走。
3. **补回来的行只能当续行，除非整行干干净净只有「词 + 词性」**。
   `ablaze adj. * đ „*x c` 就是 gapfill 补出来的乱码，还落在右栏中间。
   一刀切地不让补行开词条会丢 738 个真词头，所以加了「整行无尾巴」这个条件。

## 首字母被扫描件吃掉：规则能修，别只交给 agent
`n accepting the award`（in）、`m expecting a call`（am）、`t is accountable`（it）、
`s this an appropriate time`（is）——扫描件左边缘常把行首第一个字母切掉，剩个孤字母。
这类**全书 341 处**，我一开始只在已校对的 3535 行里让 agent 修，
未校对的 94978 行里照样在，用户一眼就看见了。

`04_expand.py::fix_lone_letter` 用固定映射修：wordfreq 只有单词频率、比不了词组，
`in` 和 `an` 分不出高下，所以直接写死 n→in、m→am、t→it、s→is、f→of、e→be、
y→by、o→to、r→or、g→go、p→up。两个坑：
- 后面跟的词卡两个字母就够（`t is accountable` 的 is 只有两个字母，卡三个会漏掉一大半）；
- 补出来的词要跟后面那个词比一下，别把 `o to` 补成 `to to`；
- **前面必须是空格或行首**，用 `\b` 会把 `couldn't believe` 里的 t 当成孤立字母，
  补成 `couldn'it believe`——我就是这么把已校对部分从 57 处搞回 97 处的，
  改完一定要重跑自查看数字有没有反弹。

**教训一：规则能覆盖全书，agent 只覆盖校对过的那部分。能写成规则的就别只让 agent 修。**

**教训二：纠错要四列都过一遍。** `g00d` 追了四轮才清干净，因为每次只补了一列：
先只纠搭配列（解释列里的没动）→ 再补解释列（例句英文没动）→ 再补例句英文
（例句中译里还混着英文碎片）。渲染时 `full`/`cn`/`en`/`zh` 都要走 `fix_phrase`。
而且纠错要放在 `if got / else` **之后**——只写在校对过的那一支里，
未校对的九万多行照样印着 `g00d coach`。

## 修不好就别印：`09_clean.py`（这一轮的主要改动）
`n accepting the award work`、`it wil work` 这两条被用户点名了好几轮还在。
根子有三个，全在「只修不丢」这个思路上：

**一、判坏词只看词频，判不出来。** `wil` 的 zipf 有 3.28，比 `ife`(2.69) 还高，
`fix_token` 的「本身不常见（<3.0）才动」直接把它放过去了。
改成**词表 + 词频两条判据合起来**（`04_expand.py::real_word`）：
`/usr/share/dict/words`（web2）里没有、词频又 <3.5，才算坏词。web2 是 1913 年的韦氏，
没有 email/website、屈折形式也缺一大半，所以还要自己还原：
屈折后缀递归剥（`floorboards`→`floorboard`→`floor`+`board`）、
双写辅音（`bogged`→`bog`）、英式拼写（`woollen`→`woolen`）、合成词（两半都得是常用词，
不然 `latural` 会被拆成 lat+ural 蒙混过关）。不还原的话 60 处 `emaciated`
会被当成 OCR 错字整行丢掉。
**`ll`↔`l` 只能做 ll→l 这一个方向**——反过来 `wil` 会变成 `will` 被认成好词，
用户点名的那条就永远修不掉。

**二、候选打平的时候在瞎猜。** 原来取词频最高的候选，`eading` 会被改成 `reading`
（其实是 leading）。改成**候选分不出高下就不改**（差距 <0.8 才算分得清），
留给清洗整行丢掉。代价是 `ost`（lost/cost/most/post）、`ight`、`oad` 这些确实修不了了。
补了一条**先验**救回大头：这本扫描件吃的几乎全是行首的 `l`，
补 `l` 的候选加 0.6 分，`ife`→life、`ong`→long 就能定下来。

**三、修不好还照印。** 新增 `09_clean.py`：搭配格子修不好**整行丢掉**，
解释和例句修不好**留空**（搭配本身是对的，行还值得印）。判据：
混中文 / 怪符号 / 组类型标记（`/ERB + ADMISSION`）/ 夹数字 / 其实是句子 /
缺首字母 / 坏词 / 两条搭配被句点黏成一条（书里的逗号被扫成了句点）。
**词头列也要过**——以前完全没纠错，印着 `thanktul adj` 这种查都查不到的词条。

丢掉 3137 行（3.2%），95427 行留下。**宁可少印一行，也不能印错一行。**

## 自查和渲染必须走同一个函数
以前 `08_audit.py` 自己抄了一份取数逻辑，渲染改了口径它还按老口径查，
报出来的 0 是假的——用户看到的错误自查里根本不出现。
现在自查直接调 `07_render.iter_rows()`，判据直接调 `09_clean.bad_phrase()`。
自查数字降到 **4 处 / 95427 行**（都是 `honour/honor` 这类拼写变体的加粗残留）。

判「例句对不对得上」和判「加粗漏没漏」也要用同一个 `hits()`，
不然一边说词在句子里、另一边说标不上，数字永远收敛不了。
加粗只查**句子里真有的那些词**：句子里压根没有的词标不上是分组问题，不是加粗的毛病。

## 自查：搭配和例句对不上，就是搭配坏了
拿「搭配里的实词有没有出现在例句里」交叉验证 4081 条已校对条目。
第一版检测报了 1438 条，大半是误报——一格有四条搭配时例句只会用其中一条，
要求全部命中太严。改成「任一条完全对上就算过」后剩 **405 条**，那才是真坏的
（`time him`、`enough without you constantly telling me how it was al bad`）。

派 agent 从 `cn` 和 `ex` 反推真正的搭配：`time him` 的解释是「时间的迷雾」、
例句是 `lost in the mists of time`，那搭配就是 `the mists of time`。
**修好 350 条，推不出来的 49 条整行删掉**。

那 49 条几乎都是**词头在 OCR 里整行丢了**，两个 agent 各自独立指认出同一批：
`weak`/`tub`/`yelp`/`T-shirt`（挂到了 way/try/year 下）、
`kerb`（挂到 keep 下）、`numerous`（挂到 number 下）、`pitch` 的棒球义
（挂到 small 下）、`badge`（挂到 bad 下）。
这些词头行 Vision 没识别、gapfill 也没补回来，搭配就顺延挂到了前一个词头名下。
判据够不着，只能整行丢——硬留着会往 way、try、number 这些词条里灌垃圾。

**这是目前最大的一类残留问题**：原书约 9000 个词头，抽到 7947，
差的那一千来个里就有不少是这种整行丢失，它们的搭配全都错挂在邻近词条下。
要根治得回到 OCR 层：对「相邻两个词头之间跨度异常大」的位置做二次识别。

## 例句里搭配要整条加粗
原来逐词标、还特意跳过虚词，结果 `a matter of time` 只有 matter 和 time 是黑的，
中间夹着没加粗的 a 和 of，看着像标漏了。改成**整条短语匹配**：
- `a/the` 这种二选一转成正则的「或」，不然 `in a/the system` 永远对不上
  例句里的 `in the system`；
- 词之间允许屈折后缀和插进一两个修饰语；
- 前后要卡词边界，否则 `in` 会匹配到 `links` 里面去；
- 整条对不上才退回逐词标（`clog system` 在例句里是 `clogging up the court system`）。
- **词尾的 e / y 要单独处理**（`09_clean.py::inflect`）：直接在词尾接 `\w{0,4}`
  的话 `ache` 匹配不上 `aching`、`carry` 匹配不上 `carried`；但把 e 一律砍掉又太松，
  `care` 会把光秃秃的 `car` 也标黑。
- **不规则动词要列表**：例句里全是 `sent` `took` `made`，跟搭配里的原形对不上，
  光这一类就漏标 300 多处。
- 例句里也常把两种拼法一起写（`They honour/honor their ancestors`），
  前后各留一个 `词/` 的口子，不然只标得住后半截。
- 一格里好几条搭配，整条标完再逐词标会套出 `<b>finished our <b>business</b></b>`，
  按深度扫一遍只留最外层。

## 眼下的质量水位
- **词头层面很干净**：独立 agent 核了两页，召回和精确都 100%。
- **短组拆得很好**（`ADV hastily | 仓促离开 + 例句`）。
- **长组还乱**，样张上看得很清楚，三种表现：
  ① ~~例句重复贴到组内每一行~~ **已修**：`take_examples` 留下的 `｜EX｜` 占位符
     标着例句在原文里的位置，切完小类按占位符个数顺序分配，谁的例句归谁。
  ② ~~例句中译一路吃到下一个小类~~ **已修**：中译必须以句末标点收尾、中间不许有
     小类分隔符。原来写成 `[^A-Z]{0,80}`，例句后面拖着
     `／ effectively, largely virtually 事实上终止…` 这种尾巴。
  ③ 中文列串进例句：`◇` 还常被认成一个孤零零的 `o`（`几乎停止o This principle…`），
     补上这个变体后残留从 6665 降到 4655，**还没清完**。
  ④ ~~断词连字符~~ **已修**：`Trad- itional` → `Traditional`、`punish– ment` →
     `punishment`。要在**跨行拼接之后**抹——断词的两半本来就在两行上，
     只看单行的 normalize 够不着。
  ⑤ 少量行带 OCR 垃圾（`computer: * PC abbreviation`）。
  根子在 OCR——原书的排版分隔信息在扫描件里本来就丢了一部分，纯规则到这儿差不多到顶。

## 单词列：合并 + 跨页重写（**自己控制分页**）
- 表头叫「单词」。
- **真正合并**：`border-collapse` 下光去掉续行格的上边框没用，上一格的下边框还在，
  横线照样一条条画。得把这一列的横线全撤掉，再单独给「换了单词」的那格补上边框。
- **不用真表格布局**。Chrome 打印时不认 `tr` 的 `break-inside: avoid`，行照样被
  分页拦腰切断（页顶经常是半句中文）。改成 `display: flex` 的伪表格，
  block 级元素的 break-inside 它是认的；列宽从 `width` 改成 `flex-basis`，
  边框也得改成每格只画右下（flex 下 border-collapse 失效）。
- **分页自己算**。JS 在 print 模拟下看到的是连续流，量不出 Chrome 把页切在哪儿；
  按页高整除也不准（它会为了不切断行上下浮动）。干脆自己来：量每行高度累加，
  超过一页正文高度就给那行打 `break-before: page`，同时
  ① 把单词补写到这一行的第一列（补完要**重新量一次行高**，补字会让行变高）；
  ② 插一份表头进去（flex 下 `thead` 不会自动重复）。
- **每页最后要封口**：单词列整列没有横线，翻页时上一页最后那格就没了底边、开着口。
  分页时给上一行的单词格补一条下边框，整张表的最后一行也补。
- 结果：样张 14 页，**14/14 页第一列顶上有单词、13/14 页有表头**，
  行零切断，每页底边横线都从 x=31.5 起（盖住第一列）。
- **改 display 之前先把旧规则清干净**：早先为了让表头每页重复写过一条
  `thead { display: table-header-group }`，改 flex 布局时忘了删，它压住了
  `thead { display: block }`——第 1 页的表头因此没走 flex，四列塌成按内容宽排，
  「单词」被挤成上下两个字，竖线冒出七条。第 2 页起用的是 JS 克隆进 tbody 的
  表头，反而是对的，一眼看不出问题在哪。
- 列宽用 `flex: 0 0 26%` 一次写全，别写成 `flex: 0 0 auto` 再补 `flex-basis`。
- 踩坑记录：验证时差点误判成没生效——第 2 页起没有大标题，正文从 y≈40 就开始，
  而我按第 1 页的排布卡了 `y > 80`，把页顶那几行全过滤掉了。

## 版面：rowspan 会让 td.cellIndex 左移
排版脚本原来按 `td.cellIndex` 取每列的行数上限和字号下限。有 rowspan 时，
**没有词头格的那些行 cellIndex 会整体左移一位**——搭配列被当成词头列，
按「最多两行、可以缩到 2.9pt」处理，于是多条搭配的格子被压成一团蚂蚁。
改成渲染时给每个 td 打 `c1`~`c4` 的 class，脚本按 class 认列。
修完样张里小于 5.5pt 的字符占比 0.0%。

另外两条：
- **每列的行数上限按内容定**：搭配列 14 行（一格能有八九条搭配，竖排本来就该占那么多），
  解释和例句 3 行，词头 2 行。宁可行高参差，也不要小到没法读。
- **词头列不能用 rowspan**：`system` 这种词条几百行，rowspan 一跨页，Chrome 会把
  那一格的边框重画一遍，第一、二列之间冒出两条竖线（x=81.0 和 81.8 并排），
  格子高度还算错、竖线直接戳出表格底边。改成每行都出一个 td，
  空的那些加 `.cont` 去掉上边框——看着照样是合并的，没有跨页毛病。
- **放不下时先拆中英换行、再考虑缩字号**。顺序反了会很难看：
  `under the new system, all children will be monitored by a social worker.`
  这种英文正好占满一行的，缩字号是把整格都缩小，而把中文接到英文后面连排，
  省下一整行、原样两行就放下了。改完 7pt 的占比从 63% 升到 94%，最小 5.8pt。
- **例句里搭配要整个加粗，不是挑几个词**。词形变化的后缀余量要给够：
  `clog` 在例句里是 `clogging`，只留 3 个字母的余量就匹配不上，
  那条搭配等于没标。占位词（sb/sth/a/the）跳过。
- **解释和例句一律压两行**：给三行的余地，脚本一看放得下就不缩字号了，
  结果 `An ancient irrigation system still waters these terraced / fields. /
  一套古老的灌溉系统仍在浇灌这些梯田。` 排成三行，`fields.` 半截单独占一行，
  而旁边几列都只有两行。宁可字号缩一点也要压平。搭配列例外（一格能有八九条）。

## 版面
- **词头列合并单元格**（rowspan），一个词头底下多少行就跨多少行。
- **搭配列允许四行**：一格常有三四条搭配（7.4% 的格子 ≥4 条），竖排本来就该占几行，
  硬压两行只会把字缩到看不清。其余列两行。
- 字号下限按列分开定：搭配列 4.8pt、中文列 4.6pt、例句列 4.4pt。

## 例句覆盖：原书就只有三成
82575 个小类里带例句的约 30%——**这是原书的样子**，牛津搭配词典本来就只给部分
搭配配例句，不是抽取漏了。要让每条搭配都有例句只能靠模型生成 14 万条，
成本是习语词典那轮的 20 倍，得先想清楚值不值。

## 「搭配疑误」几乎全是我丢的字母，不是书错
校对结果里 25% 标了「搭配疑误」。原书是权威词典，错的是我——**扫描件左边缘常把
首字母吃掉，尤其是 `l`**：`ocal`→local（134 处）、`argely`→largely（97）、
`eading`→leading（83）、`ikely`→likely（82）、`aunch`→launch、`anguage`→language，
全书 6911 处。

`04_expand.py::fix_token` 用 wordfreq 兜底修，三类候选：补首/尾字母、形近字母对
（`lefeat`→defeat、`vork`→work、`readquarters`→headquarters）、删掉多认的字母
（`gairl`→girl）。两个关键点：
- **门槛要用相对值**。卡「查不到的词」不行——`ife` 的词频有 2.69 却是 life 掉了首字母。
  改成「本身不常见（<3.0）且换法明显更常见（高出 1.5）」才动。
- **迭代两轮**。有些词错了两处：`ciress` 要先删掉多认的 i 变 cress、再把 c 认回 d。

坏词 **6911 → 1586**（-77%）。剩下的是三处以上错误或人名地名。

## 模型校对（已验证）
规则清洗到 `04_expand` 就到顶了，剩下三类毛病只能靠模型：
OCR 错字（`放 斧⋯而转向⋯` 其实是「放弃…而转向…」）、小类切分错位
（`be left ~ed` 被切成 `eft abandoned`）、例句缺七成。

已跑通：`05_batches.py`（按词频挑词头切批次）→ 子 agent 按
`prompts/INSTRUCTIONS.md` 校对 → `06_merge.py` 并入 `data/cache.json`
→ `07_render.py` 优先用校对过的。

**头 150 个高频词头（4081 条）已校完，103 个批次 0 条丢弃**，
抽取结构后来又改过一轮，按新 key 重算命中 3885 条（95%）。

**key 用「词头 + 搭配文本」，不要用位置**：位置型 id（`词头|义项|组|小类`）
一是 `name` 名词和 `name` 动词会撞车，二是抽取结构一改就全错位——
这轮改了三次抽取，要不是换成文本 key，4081 条得重做三遍。

**全量成本**：93948 条是这次的 33 倍，按每 4 个 agent 半小时跑 2800 条算，
要 130 多个 agent-批次、七八轮。建议先做高频 1000-1500 个词头（约 2.5 万条），
覆盖日常绝大多数查询。

## 下一步（两条路，成本差很多）
1. **先排 PDF 交付当前版本**：拷 `oxford-idioms/scripts/13_pdf.py`，那本的版式坑
   （量行数要 print 媒体、边框粗细 Chrome 改不动、缩字号要回弹）都记在它的 progress 里。
2. **派子 agent 逐组校对**：7.6 万个小类，是习语词典（7000 条）的 10 倍量，
   按那本的批次规模要 1000+ 个批次。真要做得先想清楚值不值，
   或者只挑长组（小类 ≥ 4 的那些）校对，量能压到十分之一。

## NOTE 用法说明框还没处理
原书有整块的用法说明框（如 flower 词条下的 `Flowers` 框），目前整块跳过，后面要补。
